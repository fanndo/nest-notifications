import { ValidationMongoIdPipe } from './validation-mongo-id.pipe';

describe('ValidationMongoIdPipe', () => {
  it('should be defined', () => {
    expect(new ValidationMongoIdPipe()).toBeDefined();
  });
});
